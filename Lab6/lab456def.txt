--Iterators, Generators, Scope, Modules, Dates, Math, JSON

Простые вопросы:
1. Как создать генератор в Python?

2. Что такое scope?

3. Что нужно сделать прежде чем воспользоваться модулем?

4. Как вывести текущую дату?

5. Как округлить число до ближайшего целого?

6. Что такое JSON

7. Какая функция math извлекает корень?

Вопросы средней сложности:
1. Как преобразовать объект Python в JSON-строку?

2. Как прочитать данные из файла JSON в Python?

3. В чем разница между функцией и генераторами?

4. Какие есть примеры встроенных модулей?

5. Как получить разницу между двумя датами в Python?

Сложные вопросы:
1. Какие математические функции и константы предоставляет модуль math для работы с тригонометрией

2. Какие есть math функции округления и в чем разница между ними?

3. Какие существуют виды scope видимости в Python и в чем их различия?





--RegEx

Простые вопросы:
1. Что такое RegEx?

2. Для чего используется модуль re в Python?

3. Как проверить, содержит ли строка подстроку, используя RegEx в Python?

4. Какой специальный символ используется, чтобы указать 0 или больше повторений подстроки; 1 или больше повторений подстроки?

5. Как разбить строку на части с помощью RegEx?

Средние по сложности вопросы:
1. Какие специальные символы вы знаете в RegEx и для чего каждый из них предназначен?

2. Как работает группировка в RegEx?

3. С помощью чего можно применить шаблон к поиску подстроки?

4. Что делает re.compile() и в чем его преимущество?

Сложные вопросы:
1. В чем разница между: "string", r"string", f"string

2. Перечислите все специальные последовательности в регулярных выражениях модуля re, которые вы знаете, и опишите, что возвращает каждая из них.





--Directories and Files, Builtin Functions of Python

Простые вопросы:
1. Как создать новую директорию в Python?

2. Как удалить файл в Python?

3. Как проверить, существует ли файл или директория в Python?

4. Как скопировать файл в Python?

5. Как получить список файлов в директории в Python?

6. Какая встроенная функция соединяет лист указанными разделителями, к примеру ", "?

7. Какая встроенная функция выводит все bool значения листа

8. Чем отличается relative path от absolute path?

Средние по сложности вопросы:
1. Как прочитать содержимое файла в Python?

2. Как записать данные в файл в Python?

3. В чем различия между "a", "r", "w", "x" в open("file.txt, _)

4. 
mylist = [10, 9, 8, 7, 6, 5]
mylist_reversed = reversed(mylist)
print(mylist_reversed)

В чем заключается ошибка в коде?

5. 
print(", ".join(map(str, nums)))
print(list(map(lambda x: x * 2, nums)))

Почему в первом случае обязательно преобразовывать в list, а во втором - нет?

Сложные вопросы:
1. Какие функции реализует вложенный в os модуль os.path? Что делает каждая из функций?

2. Как работает sleep в модуле time? Как использовать миллисекунды как аргумент к sleep?
